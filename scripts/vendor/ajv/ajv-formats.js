/* Vendored ajv-formats v3.0.1 — bundled from https://esm.sh/ajv-formats@3
   Adds string/numeric format validators (date-time, email, uri, uuid, int32/64…)
   to an Ajv instance via addFormats(ajv). No build step. */
export { default } from "./ajv-formats.mjs";
