/* Vendored Ajv v8.20.0 (JSON Schema validator) — bundled from https://esm.sh/ajv@8
   plus its fast-uri dependency. No build step; imported directly by the browser. */
export { default } from "./ajv.bundle.mjs";
