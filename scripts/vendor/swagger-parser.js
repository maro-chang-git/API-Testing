/* Vendored @apidevtools/swagger-parser v10.1.1 — bundled from
   https://esm.sh/@apidevtools/swagger-parser, with its full transitive module
   graph (incl. esm.sh node polyfills) under ./swagger-parser/. We use only
   SwaggerParser.dereference(). No build step; imported directly by the browser. */
export { default } from './swagger-parser/swagger-parser.bundle.mjs';
